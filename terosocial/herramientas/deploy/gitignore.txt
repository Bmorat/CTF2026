# =====================================================================
#  .gitignore de TeroSocial
#  (guardar como  .gitignore  en la raiz del proyecto)
#
#  Deja fuera del repo cosas pesadas o innecesarias para el servidor.
#  NO ignora entrega/ (esa carpeta SI se sube: es lo que sirve nginx).
# =====================================================================

# Fotos crudas de entrada (materia prima antes de procesar; no se sirven)
fotos_entrada/

# Version vieja del CTF, ya no aplica
_ctf_v1/

# Basura del sistema
Thumbs.db
desktop.ini
.DS_Store

# Editores
.vscode/
*.swp
*~
